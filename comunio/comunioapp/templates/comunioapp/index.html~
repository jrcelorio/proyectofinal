<html>
	<head>
		<title>Comunio</title>
	</head>
	<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
		{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>
		{% if lista_equipos %}
			<table border="2">
				<tr>
					<td>Nombre</td>
					<td>Estadio</td>
				</tr>
				{% for equipo in lista_equipos %}
					<tr>
						<td><a href="{% url 'jugadores' equipo.id %}">{{ equipo.nombre }}</a></td>
						<td>{{ equipo.descripcion }}</td>
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>No hay equipos</p>			
		{% endif %}
	</body>
</html>
